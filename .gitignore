node_modules
.next
.env
.env.local
.env.vercel-check*
*.tsbuildinfo
next-env.d.ts
.vercel
outputtestresults
eval-output
test-results
playwright-report
blob-report
.playwright

# real logged-in browser session cookies for live account inspection - never commit
.higgsfield-browser-profile/

# superpowers SDD scratch (progress ledger, briefs, reports, review packages)
.superpowers/

# graphify knowledge-graph output (dev tooling, regenerate with `graphify update .`)
graphify-out/

# typecheck-after-edit hook's runtime state (ephemeral, regenerated per session)
.claude/hooks/.typecheck-last.json
.claude/hooks/.typecheck.lock
