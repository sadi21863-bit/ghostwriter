export const dynamic = 'force-dynamic';

import { NextResponse } from "next/server";
import { getRequiredSession } from "@/lib/auth-helpers";
import { db } from "@/db";
import { projects } from "@/db/schema";
import { eq, and } from "drizzle-orm";
import { encodeAIRules } from "@/lib/types/story";
type Ctx = { params: Promise<{ projectId: string }> };
export async function GET(_: Request, { params }: Ctx){ const s=await getRequiredSession(); const p=await db.query.projects.findFirst({where:and(eq(projects.id,(await params).projectId),eq(projects.userId,s.user.id)),with:{chapters:{orderBy:(c,{asc})=>[asc(c.sortOrder)]},characters:true,locations:true,plotThreads:true,referenceWorks:true,storyMemories:true,characterRelationships:true,storyPromises:true}}); if(!p) return NextResponse.json({error:"Not found"},{status:404}); return NextResponse.json(p); }
export async function PATCH(req: Request, { params }: Ctx){ const s=await getRequiredSession(); const b=await req.json(); const { name, format, genres, notes, skillLevel, aiRules, controllingIdea, narratorVoice, narrativeStructure, qualityGradingEnabled, aiismsCheck, storyType, universeId, timelineSort, phase, seriesParentId, dismissedGuideIds, aiInitiative } = b; let encodedAiRules: ReturnType<typeof encodeAIRules> | undefined; if (aiRules !== undefined) { try { encodedAiRules = encodeAIRules(aiRules); } catch { return NextResponse.json({ error: "Invalid aiRules shape." }, { status: 400 }); } } const [u]=await db.update(projects).set({ ...(name !== undefined && { name }), ...(format !== undefined && { format }), ...(genres !== undefined && { genres }), ...(notes !== undefined && { notes }), ...(skillLevel !== undefined && { skillLevel }), ...(aiRules !== undefined && { aiRules: encodedAiRules }), ...(controllingIdea !== undefined && { controllingIdea }), ...(narratorVoice !== undefined && { narratorVoice }), ...(narrativeStructure !== undefined && { narrativeStructure }), ...(qualityGradingEnabled !== undefined && { qualityGradingEnabled }), ...(aiismsCheck !== undefined && { aiismsCheck }), ...(storyType !== undefined && { storyType }), ...(universeId !== undefined && { universeId }), ...(timelineSort !== undefined && { timelineSort }), ...(phase !== undefined && { phase }), ...(seriesParentId !== undefined && { seriesParentId }), ...(dismissedGuideIds !== undefined && { dismissedGuideIds }), ...(aiInitiative !== undefined && { aiInitiative }), updatedAt:new Date() }).where(and(eq(projects.id,(await params).projectId),eq(projects.userId,s.user.id))).returning(); return NextResponse.json(u); }
export async function DELETE(_: Request, { params }: Ctx){ const s=await getRequiredSession(); await db.delete(projects).where(and(eq(projects.id,(await params).projectId),eq(projects.userId,s.user.id))); return NextResponse.json({ok:true}); }