import Anthropic from "@anthropic-ai/sdk";
const client = new Anthropic({apiKey:process.env.ANTHROPIC_API_KEY!});
export type GenerationMode = "brainstorm"|"outline"|"write";
const MI = { brainstorm:()=>"Creative brainstorming. Wild specific ideas.", outline:(f)=>"Story architect. Detailed "+f+" outlines.", write:(f)=>"Ghostwriter. "+f+" format. Continuity." };
export async function generate({mode,prompt,context,format,maxTokens=4000}){ const msg=await client.messages.create({model:"claude-sonnet-4-20250514",max_tokens:maxTokens,system:MI[mode](format)+"\n---\n"+context,messages:[{role:"user",content:prompt}]}); const text=msg.content.filter(b=>b.type==="text").map(b=>b.text).join(""); return {text,tokensUsed:msg.usage.input_tokens+msg.usage.output_tokens,model:"claude-sonnet-4-20250514"}; }
export async function analyzeWork(title){ const msg=await client.messages.create({model:"claude-sonnet-4-20250514",max_tokens:500,messages:[{role:"user",content:'Analyze "'+title+'". Return ONLY JSON: {"Pacing":"...","Tone":"...","POV Style":"...","Dialogue Style":"...","Sentence Structure":"...","Atmosphere":"..."}'}]}); return JSON.parse(msg.content.filter(b=>b.type==="text").map(b=>b.text).join("").trim()); }
export async function generateEntity(type,prompt,ctx,existing){ const schemas={character:"name,role,age,appearance,personality,thinkingStyle,behavior,habits,fears,desires,speechPattern,backstory,arc",location:"name,description,atmosphere,history,sensoryDetails",plotThread:"name,description,status,stakes,connections"}; const userMsg=existing?"Improve:\n"+JSON.stringify(existing)+"\nReturn JSON: {"+schemas[type]+"}":prompt+"\nReturn JSON: {"+schemas[type]+"}"; const msg=await client.messages.create({model:"claude-sonnet-4-20250514",max_tokens:1500,system:"Create "+type+"s. ONLY JSON. Context: "+ctx,messages:[{role:"user",content:userMsg}]}); return JSON.parse(msg.content.filter(b=>b.type==="text").map(b=>b.text).join("").trim()); }
export async function summarizeChapter(content){ const msg=await client.messages.create({model:"claude-sonnet-4-20250514",max_tokens:500,messages:[{role:"user",content:"Summarize in 2-3 sentences for continuity:\n\n"+content}]}); return msg.content.filter(b=>b.type==="text").map(b=>b.text).join(""); }