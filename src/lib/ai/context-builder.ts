export function buildContext(p){ const r=[]; r.push("PROJECT: "+p.name+" | "+p.format+" | "+(p.genres||[]).join(", ")); if(p.referenceWorks?.length){r.push("STYLE:"); p.referenceWorks.forEach(w=>r.push('- "'+w.title+'"'));} if(p.characters?.length){r.push("CHARS:"); p.characters.forEach(c=>r.push("- "+c.name+" ("+c.role+"): "+c.personality));} if(p.locations?.length){r.push("LOCS:"); p.locations.forEach(l=>r.push("- "+l.name+": "+l.description));} if(p.plotThreads?.length){r.push("PLOTS:"); p.plotThreads.forEach(t=>r.push("- ["+t.status+"] "+t.name+": "+t.description));} return r.join("\n"); }